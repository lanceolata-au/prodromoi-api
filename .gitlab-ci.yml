stages:
    - Revise
    - Prepare
    - Build
    - Clean
    - Staging

Revision Up:
  stage: Revise
  before_script:
  - whoami
  script:
    - num=0
    - num=`expr $VERSION_REVISION + 1`
    - echo $num
    - echo "${CI_API_V4_URL}/projects/${CI_PROJECT_ID}/variables/VERSION_REVISION"
    - 'docker run alpine/curl --insecure --request PUT --header "PRIVATE-TOKEN: $API_TOKEN" "${CI_API_V4_URL}/projects/${CI_PROJECT_ID}/variables/VERSION_REVISION" --form "value=$num"'
  only:
    - /^release.*$/
    - /^hotfix.*$/
    - develop

Version Up:
  stage: Build
  before_script:
    - update-ca-certificates
  script:
    - num=0
    - num=`expr $VERSION_REVISION + 1`
    - echo $num
    - echo "${CI_API_V4_URL}/projects/${CI_PROJECT_ID}/variables/VERSION_REVISION"
    - 'docker run alpine/curl --insecure --request PUT --header "PRIVATE-TOKEN: $API_TOKEN" "${CI_API_V4_URL}/projects/${CI_PROJECT_ID}/variables/VERSION_REVISION" --form "value=$num"'
  only:
    - main

Prepare:
  stage: Prepare
  needs:
    - Revision Up
  before_script:
  - whoami
  script:
    - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
  only:
    - /^release.*$/
    - /^hotfix.*$/
    - develop

Build API:
  stage: Build
  needs:
    - Prepare
  before_script:
  - whoami
  script:
    - docker buildx create --name multiarch.api --driver docker-container
    - docker buildx build --builder multiarch.api -f Dockerfile.Api --platform linux/amd64,linux/arm64 --push -t $CI_REGISTRY/$CI_PROJECT_PATH/prodromoi-api:latest .
    - docker buildx build --builder multiarch.api -f Dockerfile.Api --platform linux/amd64,linux/arm64 --push -t $CI_REGISTRY/$CI_PROJECT_PATH/prodromoi-api:$VERSION_MAJOR.$VERSION_MINOR.$VERSION_REVISION .
  allow_failure: true
  only:
    - /^release.*$/
    - /^hotfix.*$/
    - develop

Build DbManager:
  stage: Build
  needs:
    - Prepare
  before_script:
  - whoami
  script:
    - docker buildx create --name multiarch.dbmanager --driver docker-container
    - docker buildx build --builder multiarch.dbmanager -f Dockerfile.DbManager --platform linux/amd64,linux/arm64 --push -t $CI_REGISTRY/$CI_PROJECT_PATH/prodromoi-dbmanager:latest .
    - docker buildx build --builder multiarch.dbmanager -f Dockerfile.DbManager --platform linux/amd64,linux/arm64 --push -t $CI_REGISTRY/$CI_PROJECT_PATH/prodromoi-dbmanager:$VERSION_MAJOR.$VERSION_MINOR.$VERSION_REVISION .
  allow_failure: true
  only:
    - /^release.*$/
    - /^hotfix.*$/
    - develop

Build Service:
  stage: Build
  needs:
    - Prepare
  before_script:
  - whoami
  script:
    - docker buildx create --name multiarch.service --driver docker-container
    - docker buildx build --builder multiarch.service -f Dockerfile.Service --platform linux/amd64,linux/arm64 --push -t $CI_REGISTRY/$CI_PROJECT_PATH/prodromoi-service:latest .
    - docker buildx build --builder multiarch.service -f Dockerfile.Service --platform linux/amd64,linux/arm64 --push -t $CI_REGISTRY/$CI_PROJECT_PATH/prodromoi-service:$VERSION_MAJOR.$VERSION_MINOR.$VERSION_REVISION .
  allow_failure: true
  only:
    - /^release.*$/
    - /^hotfix.*$/
    - develop

Clean:
  stage: Clean
  before_script:
  - whoami
  script: 
    - docker buildx create rm multiarch.api 
    - docker buildx create rm multiarch.dbmanager
    - docker buildx create rm multiarch.service 
  allow_failure: true
  only:
    - /^release.*$/
    - /^hotfix.*$/
    - develop

Build Service:
  stage: Build
  needs:
    - Prepare
  before_script:
  - whoami
  script:
    - docker buildx build -f Dockerfile.Service --platform linux/amd64,linux/arm64 --push -t $CI_REGISTRY/$CI_PROJECT_PATH/prodromoi-service:latest .
    - docker buildx build -f Dockerfile.Service --platform linux/amd64,linux/arm64 --push -t $CI_REGISTRY/$CI_PROJECT_PATH/prodromoi-service:$VERSION_MAJOR.$VERSION_MINOR.$VERSION_REVISION .
  only:
    - /^release.*$/
    - /^hotfix.*$/
    - develop